<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.zut.ssm.dao.mapper.DomeMapper">
    <select id="selectAll" resultType="Dome" parameterType="java.lang.String" >
        select * FROM t_dome
    </select>
    <insert id="insertdome" parameterType="Dome">
        insert into t_dome(t_username, t_password) VALUES (#{userName,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR})
    </insert>
</mapper>
